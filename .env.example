# ── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_PROJECT_NAME="Base Blast"
NEXT_PUBLIC_ONCHAINKIT_API_KEY=""
NEXT_PUBLIC_URL=""

# ── Leaderboard (onchain) ────────────────────────────────────────────────────
# Адрес задеплоенного контракта. Пока пусто — лидерборд работает в офлайн-режиме.
NEXT_PUBLIC_LEADERBOARD_ADDRESS=""
# 84532 = Base Sepolia (тест), 8453 = Base mainnet
NEXT_PUBLIC_CHAIN_ID="84532"
# (необяз.) URL CDP Paymaster для gasless-сабмита со smart wallet
NEXT_PUBLIC_PAYMASTER_URL=""

# ── Verifier (СЕКРЕТ, только сервер — НЕ коммитить реальное значение) ─────────
# Приватник доверенного подписанта. Его адрес = SIGNER_ADDRESS при деплое.
SIGNER_PRIVATE_KEY=""

# ── Deploy (contracts/, только локально для forge) ───────────────────────────
# DEPLOYER_PRIVATE_KEY=""      # деплоер (станет owner), нужен тестовый ETH в Base Sepolia
# SIGNER_ADDRESS=""            # адрес от SIGNER_PRIVATE_KEY выше
# BASE_SEPOLIA_RPC="https://sepolia.base.org"
# BASE_RPC="https://mainnet.base.org"
# BASESCAN_API_KEY=""          # для верификации контракта на Basescan
